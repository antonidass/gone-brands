version: "3.9"
services:
  postgres:
    image: 'postgres:latest'
    restart: always
    environment:
      POSTGRES_DB: "remain"
      POSTGRES_USER: "akrik"
      POSTGRES_PASSWORD: "akrik"
    ports:
      - "5432:5432"